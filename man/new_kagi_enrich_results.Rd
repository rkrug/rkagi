% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_results.R
\name{new_kagi_enrich_results}
\alias{new_kagi_enrich_results}
\title{Construct enrich results object}
\usage{
new_kagi_enrich_results(enrich, raw_json, parsed)
}
\arguments{
\item{enrich}{A prepared enrich request of class \strong{\code{kagi_enrich}}
(the object you passed to the API).}

\item{raw_json}{A length-one character string containing the raw JSON
response body returned by the API.}

\item{parsed}{A parsed list (e.g. from
\code{jsonlite::fromJSON(..., simplifyVector = FALSE)})
expected to contain top-level elements \code{meta} and \code{data}.}
}
\value{
An object of class \strong{\code{kagi_enrich_results}} with components:
\describe{
\item{\code{enrich}}{The original \code{kagi_enrich} request.}
\item{\code{json}}{Raw JSON string for reproducibility/debugging.}
\item{\code{meta}}{List with response metadata (e.g., \code{id}, \code{ms}, \code{node}, \code{api_balance}).}
\item{\code{data}}{A tibble with enrichment hits (if present). Each row typically
corresponds to an enrichment result with fields such as \code{url}, \code{title},
\code{snippet}, etc. If no results are available, this is \code{NULL}.}
}
}
\description{
Build a typed S3 object of class \strong{\code{kagi_enrich_results}} from a
successful Kagi Enrich API response. The object keeps both the
\strong{raw JSON} payload and the parsed content as a tibble for convenient
downstream use.
}
\details{
This is a low-level constructor; most users will obtain a
\code{kagi_enrich_results} via \code{enrich_perform()} or \code{kagi_enrich_once()}.
Results are coerced into a tibble for ease of inspection and
data manipulation. Any rows with missing \code{url} are dropped.
}
\examples{
\dontrun{
parsed <- list(
  meta = list(ms = 180, node = "def", api_balance = 95),
  data = list(
             list(
                t = 0,
                url = "https://example.org",
                title = "Enrich result",
                snippet = "Example..."
                )
             )
)
enrich <- new_kagi_enrich(new_kagi_connection(endpoint = "enrich"), q = "open data")
raw_json <- jsonlite::toJSON(parsed, auto_unbox = TRUE)
res <- new_kagi_enrich_results(enrich, raw_json, parsed)
res
tibble::as_tibble(res$data)
}

}
\seealso{
\code{\link{kagi_perform}},
\code{\link{new_kagi_enrich}} (request constructor)
}
