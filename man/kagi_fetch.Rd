% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kagi_fetch.R
\name{kagi_fetch}
\alias{kagi_fetch}
\title{Fetch Kagi Data into an Endpoint-Structured Project Folder}
\usage{
kagi_fetch(
  connection,
  query,
  project_folder = NULL,
  endpoint = NULL,
  overwrite = FALSE,
  workers = 1,
  limit = NULL,
  verbose = FALSE,
  error_mode = c("stop", "write_dummy")
)
}
\arguments{
\item{connection}{A \code{\link[=kagi_connection]{kagi_connection()}} object.}

\item{query}{A query object of class \verb{kagi_query_*} or a list of query
objects.}

\item{project_folder}{Root folder for endpoint-scoped outputs. If \code{NULL}, a
temporary directory is used.}

\item{endpoint}{Optional endpoint override. One of \code{"search"},
\code{"enrich_web"}, \code{"enrich_news"}, \code{"summarize"}, \code{"fastgpt"}.}

\item{overwrite}{Logical. If \code{TRUE}, endpoint output folders are overwritten.}

\item{workers}{Number of workers for list requests.}

\item{limit}{Optional integer limit used for search/enrich request calls.}

\item{verbose}{Logical indicating whether progress messages should be shown.}

\item{error_mode}{Error handling mode passed to \code{\link[=kagi_request]{kagi_request()}}.
One of \code{"stop"} or \code{"write_dummy"}.}
}
\value{
For a single endpoint, normalized parquet path. For mixed endpoint
query lists, a named list of normalized parquet paths by endpoint.
}
\description{
High-level helper that runs \code{\link[=kagi_request]{kagi_request()}} and \code{\link[=kagi_request_parquet]{kagi_request_parquet()}} in
sequence and writes outputs into endpoint-scoped project folders.
}
\details{
Folder layout:
\itemize{
\item \verb{<project_folder>/<endpoint>/json}
\item \verb{<project_folder>/<endpoint>/parquet}
}
}
\examples{
\dontrun{
conn <- kagi_connection(api_key = function() keyring::key_get("API_kagi"))
q <- query_search("biodiversity", expand = FALSE)

kagi_fetch(
  connection = conn,
  query = q,
  project_folder = "kagi_project"
)
}

}
