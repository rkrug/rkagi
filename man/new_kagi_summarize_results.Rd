% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_results.R
\name{new_kagi_summarize_results}
\alias{new_kagi_summarize_results}
\title{Construct summarize results object}
\usage{
new_kagi_summarize_results(request, raw_json, parsed)
}
\arguments{
\item{request}{A prepared summarize request of class \strong{\code{kagi_summarize}}
(the object you passed to the API).}

\item{raw_json}{A length-one character string containing the raw JSON
response body returned by the API.}

\item{parsed}{A parsed list (e.g. from \code{jsonlite::fromJSON(..., simplifyVector = FALSE)})
expected to contain top-level elements \code{meta} and \code{data}. From \code{data},
the function extracts \code{output} (as \code{summary}) and \code{tokens}.}
}
\value{
An object of class \strong{\code{kagi_summarize_results}} with components:
\describe{
\item{\code{request}}{The original \code{kagi_summarize} request.}
\item{\code{raw_json}}{Raw JSON string for reproducibility/debugging.}
\item{\code{meta}}{List with response metadata (e.g., \code{ms}, \code{node}, \code{api_balance}).}
\item{\code{summary}}{Character scalar with the summarizer output text (or \code{list()} if absent).}
\item{\code{tokens}}{Integer (or \code{list()} if absent) with token usage reported by the API.}
}
}
\description{
Build a typed S3 object of class \strong{\code{kagi_summarize_results}} from a
successful Universal Summarizer response. The object keeps both the
\strong{raw JSON} payload (as returned by the API) and the \strong{parsed} fields
you will typically use (\code{meta}, \code{summary}, \code{tokens}).
}
\details{
This is a low-level constructor; most users will obtain a
\code{kagi_summarize_results} via \code{kagi_perform()} (or your high-level
wrapper). The fields use \verb{\%||\%} to provide safe fallbacks when the API
omits optional elements.
}
\examples{
\dontrun{
parsed <- list(
  meta = list(ms = 123, node = "xyz", api_balance = 42),
  data = list(output = "Short summary...", tokens = 256L)
)
req <- new_kagi_summarize(new_kagi_connection(endpoint = "summarize"), text = "Lorem ipsum")
res <- new_kagi_summarize_results(req, raw_json = jsonlite::toJSON(parsed), parsed = parsed)
res
}

}
\seealso{
\code{\link{kagi_perform}},
\code{\link{new_kagi_summarize}} (request constructor),
}
