% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kagi_request.R
\name{kagi_request}
\alias{kagi_request}
\title{Execute Kagi API requests and save JSON responses}
\usage{
kagi_request(
  connection,
  query,
  limit = NULL,
  pages = 1,
  output = NULL,
  overwrite = FALSE,
  append = FALSE,
  workers = 1,
  verbose = FALSE,
  error_mode = c("stop", "write_dummy"),
  metadata_request_args = list()
)
}
\arguments{
\item{connection}{A \code{\link[=kagi_connection]{kagi_connection()}} object.}

\item{query}{A query object of class \code{kagi_query_search},
\code{kagi_query_extract}, or a list of such objects.}

\item{limit}{Optional integer limit used for search requests.}

\item{pages}{Integer between 1 and 10. Number of pages to be downloaded
per query. The Kagi \verb{/search} endpoint is body-paginated; each iteration
sets \code{body$page} to the page index and writes a separate
\verb{search_<page>.json} file. Ignored for \code{kagi_query_extract} (one shot).}

\item{output}{Directory where JSON response files are written.}

\item{overwrite}{Logical. If \code{TRUE}, existing \code{output} is deleted before
writing.}

\item{append}{Logical. If \code{TRUE}, write into an existing \code{output} directory
instead of deleting it.}

\item{workers}{Number of parallel workers to use when \code{query} is a list.}

\item{verbose}{Logical indicating whether progress messages should be shown.}

\item{error_mode}{Error handling mode. \code{"stop"} (default) throws on request
errors. \code{"write_dummy"} writes a fallback JSON payload and returns \code{output}.}

\item{metadata_request_args}{Optional named list persisted in replay metadata
(\code{request_args}) for each query. Intended for higher-level orchestrators.}
}
\value{
The normalized path to \code{output}.
}
\description{
Execute one or more \code{kagiPro} query objects against the Kagi API and write
raw JSON responses to disk. Supports \code{\link[=kagi_query_search]{kagi_query_search()}} and \code{\link[=kagi_query_extract]{kagi_query_extract()}}.
}
\details{
If \code{query} is a list of query objects, requests are executed in parallel
(using \code{workers}) and each query is written into a named subdirectory under
\code{output}.

Files are written as \verb{\{endpoint\}_\{page\}.json} (for example \code{search_1.json}).
Pagination for \verb{/search} is body-driven: the caller controls how many pages
to fetch via the \code{pages} argument (1–10), and each iteration sets the
\code{page} field of the request body.

Query replay metadata is written alongside JSON outputs:
\itemize{
\item per query folder: \verb{_query_meta.json}
}
}
