% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_corpus.R
\name{read_corpus}
\alias{read_corpus}
\title{Read a kagiPro Parquet Corpus}
\usage{
read_corpus(
  project_folder,
  endpoint,
  corpus = "parquet",
  return_data = FALSE,
  abstracts = FALSE,
  silent = FALSE
)
}
\arguments{
\item{project_folder}{Root project folder.}

\item{endpoint}{Endpoint folder name under `project_folder`.}

\item{corpus}{Folder name under `project_folder/endpoint` to read as parquet
corpus. Defaults to `"parquet"`.}

\item{return_data}{Logical; if `TRUE`, collect and return in-memory data.}

\item{abstracts}{Logical; if `TRUE`, link sibling abstract data by `id` and
`query`.}

\item{silent}{Logical; if `TRUE`, suppress informative messages.}
}
\value{
An Arrow dataset/query when `return_data = FALSE`, otherwise a data
  frame/tibble.
}
\description{
Modelled on `openalexPro::read_corpus()` with an additional
`abstracts` switch. By default this opens an Arrow dataset from a parquet
directory. When `return_data = TRUE`, the result is collected into memory.
}
\details{
If `abstracts = TRUE`, abstract data is read from the sibling
`abstract` folder and left-joined by `id` + `query`. If no abstract files are
present, an `abstract` column filled with `NA` is added.
}
