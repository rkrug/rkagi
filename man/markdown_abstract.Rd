% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markdown_abstract.R
\name{markdown_abstract}
\alias{markdown_abstract}
\title{Summarize Markdown into Query-Level Abstract Parquet}
\usage{
markdown_abstract(
  project_folder,
  endpoint = NULL,
  query_name = NULL,
  workers = 4,
  progress = interactive(),
  verbose = FALSE,
  summarizer_fn = summarize_with_openai,
  model = "gpt-4.1-mini",
  provider_args = list(),
  markdown_root = "markdown",
  abstract_root = "abstract"
)
}
\arguments{
\item{project_folder}{Root project folder containing endpoint subfolders.}

\item{endpoint}{Optional endpoint selector (currently only `"search"`).
If `NULL`, all supported endpoints are considered.}

\item{query_name}{Optional query selector. If `NULL`, all query partitions
are considered.}

\item{workers}{Number of parallel workers to use for summarization.}

\item{progress}{Logical indicating whether progress messages should be shown.}

\item{verbose}{Logical indicating whether detailed messages should be shown.}

\item{summarizer_fn}{Function with signature
`fn(text, model, ...) -> character(1) | NA_character_`.}

\item{model}{Provider-specific model/engine.}

\item{provider_args}{Optional named list forwarded to `summarizer_fn`.}

\item{markdown_root}{Root folder name containing markdown files.}

\item{abstract_root}{Root folder name for abstract parquet outputs.}
}
\value{
Invisibly returns a data frame with columns
  `endpoint`, `id`, `query`, `abstract`, `status`, `error`.
}
\description{
Read markdown files generated for a specific endpoint/query and summarize
each record with either OpenAI or Kagi text summarization. The result is
written as a single parquet file per query under `abstract/`.
}
