% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_kagi_search_once.R
\name{kagi_search_once}
\alias{kagi_search_once}
\title{Perform a Kagi search in one call}
\usage{
kagi_search_once(
  q,
  limit = NULL,
  api_key = Sys.getenv("KAGI_API_KEY"),
  base_url = "https://kagi.com/api/v0",
  path = NULL
)
}
\arguments{
\item{q}{Character scalar. The search query string. This can include
search operators such as \code{filetype:pdf}, \code{site:example.com},
boolean logic (\code{AND}, \code{OR}), quoted phrases, etc.}

\item{limit}{Optional integer. Maximum number of results to request.
If \code{NULL} (default), the API will return its own default number of hits.}

\item{api_key}{API key to use for authentication. By default this is
read from the \code{KAGI_API_KEY} environment variable. For best practice,
set \code{Sys.setenv(KAGI_API_KEY="...")} or add it to your \verb{~/.Renviron}.
Advanced users can also pass a function that resolves the key lazily
(see \code{\link[=new_kagi_connection]{new_kagi_connection()}}).}

\item{base_url}{Base URL for the Kagi API. Defaults to
\code{"https://kagi.com/api/v0"}. Override only if you are testing against
a mock server or a future API version.}

\item{path}{Optional file path. If supplied, the raw JSON response will
be written to this path for reproducibility or debugging.}
}
\value{
An object of class \strong{\code{kagi_search_results}}. Use helper
functions to extract components:
\describe{
\item{\code{kagi_hits()}}{Return a tibble of primary search hits.}
\item{\code{kagi_related()}}{Character vector of related queries.}
\item{\code{kagi_meta()}}{List of response metadata (latency, node, API balance).}
}
}
\description{
This is a high-level convenience wrapper around
\code{\link[=new_kagi_connection]{new_kagi_connection()}}, \code{\link[=new_kagi_search]{new_kagi_search()}}, and \code{\link[=kagi_perform]{kagi_perform()}}.
It constructs a connection, prepares a search request, and performs
it in a single step.
}
\examples{
\dontrun{
# Simple one-liner
res <- kagi_search_once("openalex api", limit = 3)
kagi_hits(res)

# With a complex query
q <- build_kagi_q(query = "biodiversity",
                  filetype = c("pdf", "docx"),
                  site = c("example.com", "gov"))
res <- kagi_search_once(q, limit = 5)
kagi_hits(res)
}

}
\seealso{
\code{\link[=new_kagi_connection]{new_kagi_connection()}},
\code{\link[=new_kagi_search]{new_kagi_search()}},
\code{\link[=kagi_perform.kagi_search]{kagi_perform.kagi_search()}},
\code{\link[=kagi_hits]{kagi_hits()}},
\code{\link[=kagi_related]{kagi_related()}},
\code{\link[=kagi_meta]{kagi_meta()}}
}
