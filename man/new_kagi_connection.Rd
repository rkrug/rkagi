% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{new_kagi_connection}
\alias{new_kagi_connection}
\title{Construct a Kagi API connection}
\usage{
new_kagi_connection(
  base_url = "https://kagi.com/api/v0",
  endpoint = as.character(NA),
  api_key = Sys.getenv("KAGI_API_KEY"),
  max_tries = 3
)
}
\arguments{
\item{base_url}{Character scalar. Base URL for the Kagi API.
Defaults to \code{"https://kagi.com/api/v0"}.}

\item{endpoint}{Character scalar. Specific endpoint to use
(e.g. \code{"search"}, \code{"enrich"}, \code{"summarize"}). Must be non-empty.}

\item{api_key}{API key used for authentication. By default this is read
from the environment variable \code{KAGI_API_KEY}. Best practice is to set
this variable in your \verb{~/.Renviron}. Advanced users may also supply
a function that resolves the key lazily at request time
(see \code{\link[=resolve_api_key]{resolve_api_key()}}).}

\item{max_tries}{Integer scalar. Maximum number of retry attempts
for transient errors. Defaults to \code{3}.}
}
\value{
An object of class \strong{\code{kagi_connection}} with components:
\describe{
\item{\code{base_url}}{Base API URL.}
\item{\code{endpoint}}{Endpoint string.}
\item{\code{api_key}}{API key (or a function to resolve it).}
\item{\code{max_tries}}{Maximum retry attempts.}
}
}
\description{
Build a typed S3 object of class \strong{\code{kagi_connection}} which holds the
basic configuration required to talk to the Kagi API. This includes
the API base URL, endpoint, authentication key, and retry settings.
}
\details{
This is a low-level constructor; most users will not call it directly.
Instead, use higher-level helpers such as \code{\link[=kagi_search_once]{kagi_search_once()}},
\code{\link[=kagi_enrich_once]{kagi_enrich_once()}}, or \code{\link[=kagi_summarize_once]{kagi_summarize_once()}}, which create
connections internally.
}
\examples{
\dontrun{
# Basic connection (API key from env var)
conn <- new_kagi_connection(endpoint = "search")
conn

# Explicit API key
conn2 <- new_kagi_connection(endpoint = "search", api_key = "my-key")

# Lazy API key via keyring
conn3 <- new_kagi_connection(endpoint = "search",
                             api_key = function() keyring::key_get("API_kagi"))
}

}
\seealso{
\code{\link[=resolve_api_key]{resolve_api_key()}},
}
