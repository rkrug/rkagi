% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_kagi_summarize_once.R
\name{kagi_summarize_once}
\alias{kagi_summarize_once}
\title{Perform a Kagi summarization in one call}
\usage{
kagi_summarize_once(
  url = NULL,
  text = NULL,
  engine = NULL,
  summary_type = NULL,
  target_language = NULL,
  cache = NULL,
  api_key = Sys.getenv("KAGI_API_KEY"),
  base_url = "https://kagi.com/api/v0",
  path = NULL
)
}
\arguments{
\item{url}{Optional character scalar. URL to be summarized. Mutually exclusive
with \code{text}. If both are provided, \code{text} takes precedence.}

\item{text}{Optional character scalar. Raw text to be summarized.
Mutually exclusive with \code{url}.}

\item{engine}{Optional character scalar. Summarizer engine to use (e.g.
\code{"muriel"}, \code{"cecil"}). If \code{NULL}, the API default is used.}

\item{summary_type}{Optional character scalar. Type of summary requested
(e.g. \code{"summary"}, \code{"takeaway"}). If \code{NULL}, the API default is used.}

\item{target_language}{Optional character scalar. Target language for
the summary (ISO code such as \code{"EN"}, \code{"FR"}, \code{"DE"}, etc.).
If \code{NULL}, the API default is used.}

\item{cache}{Optional logical. Whether to use cached summarizations on the API side.
If \code{NULL}, the API default is used.}

\item{api_key}{API key to use for authentication. By default this is
read from the \code{KAGI_API_KEY} environment variable. For best practice,
set \code{Sys.setenv(KAGI_API_KEY="...")} or add it to your \verb{~/.Renviron}.
Advanced users can also pass a function that resolves the key lazily
(see \code{\link[=new_kagi_connection]{new_kagi_connection()}}).}

\item{base_url}{Base URL for the Kagi API. Defaults to
\code{"https://kagi.com/api/v0"}. Override only if you are testing against
a mock server or a future API version.}

\item{path}{Optional file path. If supplied, the raw JSON response will
be written to this path for reproducibility or debugging.}
}
\value{
An object of class \strong{\code{kagi_summarize_results}}. Key components include:
\describe{
\item{\code{tokens}}{Token usage information.}
\item{\code{meta}}{List of response metadata (latency, node, API balance).}
}
}
\description{
This is a high-level convenience wrapper around
\code{\link[=new_kagi_connection]{new_kagi_connection()}}, \code{\link[=new_kagi_summarize]{new_kagi_summarize()}}, and \code{\link[=kagi_perform]{kagi_perform()}}.
It constructs a connection, prepares a summarization request, and performs
it in a single step.
}
\examples{
\dontrun{
# Summarize a URL
res <- kagi_summarize_once(
  url = "https://www.youtube.com/watch?v=ZSRHeXYDLko",
  engine = "muriel",
  summary_type = "summary",
  target_language = "EN"
)

# Summarize raw text
res <- kagi_summarize_once(
  text = "Long input text to be summarized...",
  engine = "cecil",
  summary_type = "takeaway"
)
}

}
\seealso{
\code{\link[=new_kagi_connection]{new_kagi_connection()}},
\code{\link[=new_kagi_summarize]{new_kagi_summarize()}},
\code{\link[=kagi_perform]{kagi_perform()}}
}
