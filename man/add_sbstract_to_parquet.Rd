% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_sbstract_to_parquet.R
\name{add_sbstract_to_parquet}
\alias{add_sbstract_to_parquet}
\title{Add Abstract to Search/Enrich Parquet}
\usage{
add_sbstract_to_parquet(
  connection,
  input_parquet,
  output = input_parquet,
  overwrite = FALSE,
  workers = 1,
  engine = "cecil",
  summary_type = "summary",
  target_language = "EN",
  cache = TRUE,
  error_mode = c("stop", "write_dummy"),
  summarize_output = NULL,
  endpoint = NULL,
  verbose = FALSE
)
}
\arguments{
\item{connection}{A \code{\link[=kagi_connection]{kagi_connection()}} object.}

\item{input_parquet}{Directory containing parquet files generated by
\code{\link[=kagi_request_parquet]{kagi_request_parquet()}}.}

\item{output}{Directory for the augmented parquet dataset. Default is
\code{input_parquet} (in-place rewrite).}

\item{overwrite}{Logical. If \code{TRUE}, overwrite \code{output} when it exists.}

\item{workers}{Number of workers used by \code{\link[=kagi_request]{kagi_request()}} for summarize calls.}

\item{engine}{Summarize engine (\code{"cecil"}, \code{"agnes"}, \code{"muriel"},
\code{"daphne"}).}

\item{summary_type}{Summarize mode (\code{"summary"} or \code{"takeaway"}).}

\item{target_language}{Target language code (default \code{"EN"}).}

\item{cache}{Logical passed to \code{\link[=query_summarize]{query_summarize()}}.}

\item{error_mode}{Error handling mode passed to \code{\link[=kagi_request]{kagi_request()}}. One of
\code{"stop"} or \code{"write_dummy"}.}

\item{summarize_output}{Optional directory to persist summarize JSON payloads.
If \code{NULL}, a temporary directory is used.}

\item{endpoint}{Optional explicit endpoint override. If \code{NULL}, auto-detected.
Allowed values: \code{"search"}, \code{"enrich"}, \code{"enrich_web"},
\code{"enrich_news"}.}

\item{verbose}{Logical indicating whether progress messages should be shown.}
}
\value{
Normalized path to the augmented parquet output directory.
}
\description{
Read a parquet dataset created by \code{\link[=kagi_request_parquet]{kagi_request_parquet()}}, auto-detect the
endpoint type, call the Kagi Summarize endpoint for each unique URL (for
Search and Enrich datasets), and write an augmented parquet dataset with an
\code{Abstract} column.
}
\details{
Supported endpoint types for abstract augmentation are Search and Enrich
(including enrich news/web outputs). For other endpoint types, the function
errors.
}
\examples{
\dontrun{
out <- add_sbstract_to_parquet(
  connection = conn,
  input_parquet = "search_parquet",
  output = "search_with_abstract",
  overwrite = TRUE
)
}

}
